# このリポジトリでの共通ルール

すべてのリポジトリに同じ内容を置いている。1箇所を直したら他にも反映すること。

## セッションの引き継ぎルール

- 会話が長くなった場合、または一つの大きなタスクが一段落した場合は、ユーザーにセッションリセット(`/clear`)を促すこと。
- リセットを促す際は、必ず次のセッションへの引き継ぎ内容(現在の進捗、次にやるべきこと、維持すべき仕様、未解決の課題)をまとめ、プロジェクトルートの `.claude_session.md` にファイルとして出力(または更新)すること。
- ファイルの出力が完了したら、ユーザーに `/clear` の実行を促すとともに、新しいセッション開始時にユーザーが忘れないよう、「リセットが完了したら、以下の文言をコピーしてそのまま入力してください」と伝えて、以下のプロンプト例をコードブロック形式で目立たせて提示すること。

```markdown
.claude_session.md を読んで、前回の続きから開発を再開してください
```

## 説明のしかた

- 専門用語(リポジトリ、APIなど)が出た際は、その都度カッコ書きで平易に解説する(AIツール自体の基本操作の説明は不要)。
- 結論を最初に述べ、理由・詳細はその後に続ける。冗長な説明は避ける。
- 記号や文字を組み合わせた簡単な図(アスキーアート)を積極的に使う。
- 大きな作業に着手する前は、まず計画を提示し、ユーザーの合意を得てから実装する。
- 成果物を出す前に、論理の飛躍・根拠の明示・数値や固有名詞の誤り・表記の統一・過不足をセルフチェックする。

## ファイル操作

- ファイルの削除は禁止。必要な場合は事前に確認し、完全消去ではなくゴミ箱フォルダへの移動で対応する。
